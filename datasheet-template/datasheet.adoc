= FT9xx Series 32-Bit Programming Guide
:author: Software Engineering Team
:email: paul.jiao@brtchip.com
:revnumber: v0.1
:revdate: 2026-02-11
:toc: 
:toclevels: 3
:sectnums:
:sectnumlevels: 3
:imagesdir: ./images
:icons: font
:figure-caption: 图
:table-caption: 表
:source-highlighter: rouge
:stem: asciimath
:docinfo: shared
:wavedrom:

== 概述 (Introduction)
本文档使用一个嵌入式软件手册的场景，演示如何用 AsciiDoc 编写结构化技术文档。它既是一个示例文档，也是一个可复用的教程模板。

本教程面向以下读者：

* 需要编写芯片数据手册、寄存器说明或编程指南的技术写作者
* 需要维护驱动文档、测试脚本说明或接口说明的嵌入式软件工程师
* 希望将文档拆分为模块化章节、统一样式并输出 HTML/PDF 的团队

[IMPORTANT]
====
在操作高压外设前，请确保 VDD 处于 3.3V 稳定状态。
====

== 教程结构 (Tutorial Map)

本示例按常见技术文档写作流程组织：

* 先定义写作规范，统一公式、图片和格式
* 再展示代码示例的写法与复用方式
* 然后给出寄存器映射与详细寄存器描述
* 最后补充公式、时序图、框图与修订记录

这样安排的目的，是让新作者先掌握文档约束，再学习具体内容模块如何落地。

== 写作规范 (Authoring Conventions)

本节用于约束团队写作方式。对于教程模板来说，写作规范应尽量明确，避免“文档能写出来，但每个人写法都不同”的情况。

include::chapters/conventions.adoc[leveloffset=+1]

== 代码示例 (Code Examples)

技术教程通常需要同时服务“阅读者”和“维护者”。因此，示例代码不仅要能看懂，也应尽量接近真实工程中的可维护形式。

include::chapters/python_code.adoc[leveloffset=+1]

== 寄存器映射 (Register Maps)
寄存器映射表适合放在靠前位置，帮助读者快速理解模块范围、偏移地址和访问属性。

[cols="2,1,1,4", options="header"]
|===
| Register Name | Offset | Access | Description
| GPIO_MODE    | 0x00   | R/W    | 配置 GPIO 引脚模式
| GPIO_ODATA   | 0x04   | R/W    | 输出数据寄存器
| GPIO_IDATA   | 0x08   | R      | 输入数据寄存器
|===

== 外设编程实例 (Peripheral Programming)

在寄存器总览之后给出最小可读的初始化示例，能够帮助读者把“寄存器定义”与“实际使用方式”联系起来。

=== UART 初始化

以下是基于 *C 语言* 的 UART 初始化示例：

[source,c]
----
#include "mcu_uart.h"

void uart_init(uint32_t baudrate) {
    UART->BAUD = SystemCoreClock / baudrate; // 设置波特率
    UART->CTRL |= UART_ENABLE;               // 使能串口
}
----

== 内容复用 (Modular Content)

较长的编程手册通常会把寄存器描述、接口章节或示例章节拆分到独立文件，再通过 `include::` 统一组装。这种方式更利于多人协作和版本维护。

include::regs/_ctrl_reg.adoc[leveloffset=+1]

== 公式与计算 (Formulas)

本模板推荐使用 AsciiMath 来表达公式，以便在 HTML、PDF 和其他转换场景中保持更稳定的兼容性。

对于 PWM 频率的计算，推荐写法如下：

[asciimath]
++++
f_(PWM) = f_(clk) / ((Prescaler + 1) * (Period + 1))
++++

行内公式也可以直接写为 stem:[f_(PWM) = f_(clk) / ((Prescaler + 1) * (Period + 1))]。

== 寄存器描述示例 (Register Description Example)

=== GPIO_MODE Register (0x00)

==== Register Layout (32-bit)

[cols="1,4,1,6", options="header"]
|===
| Bits | Field | Reset | Description
| 31:16 | RESERVED | 0x0000 | Reserved, must keep default value.
| 15:8  | SPEED    | 0x00   | Output speed selection.
| 7:4   | PULL     | 0x0    | Pull-up / pull-down configuration.
| 3:0   | MODE     | 0x0    | Pin mode selection.
|===

==== Field Definitions

* *MODE* (Bits [3:0])
** `0x0` = Input
** `0x1` = Output
** `0x2` = Alternate Function
** `0x3` = Analog

* *PULL* (Bits [7:4])
** `0x0` = No pull
** `0x1` = Pull-up
** `0x2` = Pull-down

* *SPEED* (Bits [15:8])
** `0x00` = Low
** `0x01` = Medium
** `0x02` = High
** `0x03` = Very High

==== C Access Example

[source,c]
----
#include <stdint.h>

#define GPIO_BASE      (0x40020000UL)
#define GPIO_MODE_OFF  (0x00UL)
#define GPIO_MODE      (*(volatile uint32_t *)(GPIO_BASE + GPIO_MODE_OFF))

// Bit positions / masks
#define GPIO_MODE_MODE_Pos   0
#define GPIO_MODE_MODE_Msk   (0xFUL << GPIO_MODE_MODE_Pos)

#define GPIO_MODE_PULL_Pos   4
#define GPIO_MODE_PULL_Msk   (0xFUL << GPIO_MODE_PULL_Pos)

#define GPIO_MODE_SPEED_Pos  8
#define GPIO_MODE_SPEED_Msk  (0xFFUL << GPIO_MODE_SPEED_Pos)

static inline void gpio_mode_set(uint32_t mode, uint32_t pull, uint32_t speed)
{
    uint32_t reg = GPIO_MODE;

    reg &= ~(GPIO_MODE_MODE_Msk | GPIO_MODE_PULL_Msk | GPIO_MODE_SPEED_Msk);
    reg |= ((mode  & 0xFUL)  << GPIO_MODE_MODE_Pos);
    reg |= ((pull  & 0xFUL)  << GPIO_MODE_PULL_Pos);
    reg |= ((speed & 0xFFUL) << GPIO_MODE_SPEED_Pos);

    GPIO_MODE = reg;
}

static inline uint32_t gpio_mode_get(void)
{
    return GPIO_MODE;
}

----

[discrete]
== Notes

* This register is 32-bit aligned, so offsets increment by 4 bytes (0x00, 0x04, 0x08...).
* Always preserve *RESERVED* bits (read-modify-write) unless the spec says otherwise.

== 波形设计 (Timing Diagrams)

时序图适合用于协议说明、采样边沿说明以及初始化流程中的关键时序约束。以下示例使用 WaveDrom 表达 SPI 数据传输关系：

[wavedrom,format=svg]
----
{ "signal": [
  { "name": "SCK",  "wave": "p......" },
  { "name": "MOSI", "wave": "x.3456..", "data": ["A2", "A6", "A5", "A4"] },
  { "name": "MISO", "wave": "x......",  "data": ["D7"] },
  { "name": "CSN",  "wave": "1.0...1." }
]}
----

== 架构框图 (Architecture Diagrams)

对于模块关系、总线连接和数据流向，PlantUML 是一种适合版本管理的文本化画图方式。

[plantuml, arch_diagram, svg]
----
@startuml
skinparam componentStyle rectangle

package "MCU Core" {
  [Cortex-M Processor] as CPU
}

package "SPI Peripheral" {
  [Register Interface] as REG
  [FIFO Buffer] as FIFO
  [State Machine] as FSM
  [Baud Rate Gen] as BRG
}

node "External Flash" as Flash

CPU <-> REG : APB Bus
REG <-> FIFO
FIFO <-> FSM
BRG -> FSM
FSM <-> Flash : SPI Bus
@enduml
----

如果希望直接复用已有图片资源，也可以插入静态图像：

[.figcap-center]
.System Diagram (系统架构框图) 

[align="center"]
image::BT820architecture.png[width=600]

[discrete]
== 修订记录 (Revision History)

对于模板项目，修订记录既可以作为正式出版内容的一部分，也可以作为团队内部示例，用来说明版本信息该如何组织。

[cols="1,1,2,6", options="header"]
|===
| 日期 | 版本 | 作者 | 变更说明
| 2026-02-12 | v0.1 | Paul Jiao | 初稿：新增寄存器章节与位域图示
| 2026-02-15 | v0.2 | KH Low | 修正章节编号与 TOC 显示
|===
